services:
  db:
    image: postgres:16-alpine
    container_name: huminloop-db
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: huminloop
      POSTGRES_USER: huminloop
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-huminloop_dev}
    volumes:
      - huminloop_data:/var/lib/postgresql/data
      - ./docker/init.sql:/docker-entrypoint-initdb.d/01-init.sql
    deploy:
      resources:
        limits:
          cpus: "4.0"
          memory: 4G
        reservations:
          cpus: "2.0"
          memory: 2G
    shm_size: 256mb
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U huminloop"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  huminloop_data:
    driver: local
